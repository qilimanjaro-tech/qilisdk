Fixed OpenQASM 2.0 import parsing by replacing parameter parsing with AST-based expression evaluation (supporting `pi`, arithmetic, and standard functions like `sin`/`cos`/`sqrt`), handling inline `//` comments, and adding stricter gate-parameter parsing with clear errors for invalid/deeply nested expressions; expanded unit tests to cover these cases and full supported gate round-trips.
