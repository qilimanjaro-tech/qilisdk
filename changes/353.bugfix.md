Fixed a bug in which Circuits could not be hashed if they contained a measurement.

Also tweaked the Circuit hash so that it follows the `Circuit.__eq__` method closer with respect to Parameters, such that now both are rounded to the same `atol` when compared.
