The `missing_translations.sh`script used to check for missing translation entries has been updated to use an actual standard translation tool (`msgfmt`) rather than a regex which always seemed to have problems.

Given this, a variety of missing translations have been fixed.
