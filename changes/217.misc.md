The performance of post-processing CUDA samples has been improved. When calculating an observable cost function from samples, previously .get_probabilities() was used which if the results contained a state (as per the output of CUDA dynamics), would be 2^n values, rather than the ~1000 samples.

Additionally, one can now force CPU use in the CUDA backend (useful for benchmarking).
