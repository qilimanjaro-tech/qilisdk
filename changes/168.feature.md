QTensor has been moved to C++, giving increased performance.

The interface remains almost exactly the same, the only change being that now `QTensor * QTensor` does element-wise multiplication, for matrix multiplication please change to using `QTensor @ QTensor`.
