`Model.ising()` and `Model.random_ising()` now return a model featuring SpinVariables rather than BinaryVariables, to better fit the literature. In doing so, the classical solvers have been updated to allow for optimization over spins, and SpinVariable has been given a `.to_binary()` like the other variable types to help conversion.
