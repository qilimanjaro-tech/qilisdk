Added maximum limits to some `BackendConfig` fields on the Python side, since if the user gave a value larger than a C++ integer (`> 2**32`) then it could cause a seg fault when the values got passed.
