Dropped the MKL-linked numpy and scipy builds and the unused `mkl-service` dependency. numpy and scipy now come from PyPI on every platform, which also removes `mkl`, `onemkl-license`, `intel-openmp`, `intel-cmplr-lib-ur`, `tbb`, `tcmlib` and `umf` from installs on Linux and Windows x86_64.
