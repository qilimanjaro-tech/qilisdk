Fixed a typo in `conf.py` which would cause the `to_yaml` and `from_yaml` methods to not be skipped in the API docs are they were supposed to be. Added tests for the `conf.py` since previously there weren't any.
