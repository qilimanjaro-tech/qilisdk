The concrete experiment types and their result classes are no longer exposed from `qilisdk.experiments`; they have been moved into a private library. Only the base classes `ExperimentFunctional`, `ExperimentResult` and `Dimension` remain public.
