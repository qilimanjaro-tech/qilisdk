Introduced a digital circuit transpiler with a built-in pass that decomposes any multi-controlled single-qubit gate into native operations, enabling both CUDA and QuTiP backends to execute circuits containing such constructs.
