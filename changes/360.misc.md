The time taken to build and run the tests and other checks via GitHub actions has been reduced. This is mostly to speed up the dev workflow, which previous required waiting for 5 minutes for checks, which has now been reduced to 4 without sacrificing quality of tests. The max time limit for the tests has also been increased to 10 minutes, just in case.
